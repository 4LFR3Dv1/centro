{
  "name": "centro",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "data:ingest": "node scripts/ingest-detran-v2.mjs",
    "build:app": "tsc --noEmit && vite build",
    "build": "npm run data:ingest && npm run build:app",
    "preview": "vite preview",
    "check": "npm run data:ingest -- --strict && npm run build:app"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "csv-parse": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-router-dom": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest"
  }
}
