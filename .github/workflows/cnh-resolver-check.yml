name: CNH resolver check

on:
  push:
    branches:
      - feat/cnh-resolver-003
    paths:
      - 'public/cnh-journey.js'
      - 'public/cnh-journey.css'
      - '.github/workflows/cnh-resolver-check.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - name: Validate resolver JavaScript
        run: node --check public/cnh-journey.js
      - name: TypeScript + Vite
        run: npm run build:app
